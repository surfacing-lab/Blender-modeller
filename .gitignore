# Blender auto-saved backups
*.blend1
*.blend2

__pycache__/
*.pyc

# Reference images extracted from the packed .blend. NOT committed: checked via
# the API and this repository is PUBLIC, so committing Optic output from a
# Toyota tenant would publish it irreversibly. Needs an explicit decision made
# in knowledge of that, not a default.
refs/*.png
